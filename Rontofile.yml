# Section defaults
version: 1
defaults:
  MANIFEST: 'default.xml'
docker:  # if defined this requests docker
  # image: almedso/yocto-bitbaker:latest # the default
  # indicates that a privatized image is to be used
  privatized_image:  # my-yocto-bitbaker # the default
  root_dir:
    extern:
    # intern: /yocto/root
#repo:
#  url: git@github.com:almedso/repo-yocto.git
git:
build:
  # init_script: foo-bar.sh
  # template_dir: bar-foo/bla
  # build_dir: bingo
  site:
    overwrite: True
    # file: site.conff